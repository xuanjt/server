     const user = new User(response.data);
      
      console.log(user)
      setCurrentUser(user);

      console.log(currentUser);
为什么用这种方式currentUser不是null

而用下面这种方式currentUser 就是null
     const user = new User(response.data);
      
      setCurrentUser(user);

      console.log(currentUser);